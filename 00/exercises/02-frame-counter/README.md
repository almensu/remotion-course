# 00-02｜Frame Counter：让时间轴变得可见

按以下顺序完成：

1. 阅读 `task.md`，先写预测。
2. 检查 `params.json` 的单位与语义。
3. 使用 `prompt.md` 让AI生成第一版代码。
4. 按 `harness.md` 检查是否违反本轮边界。
5. 对照 `solution.tsx`，只比较因果结构，不抄样式。
6. 完成 `audit.md` 后才算通过。

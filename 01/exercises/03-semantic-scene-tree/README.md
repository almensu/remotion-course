# 01-03｜Semantic Scene Tree：按画面职责拆组件

完成顺序：

1. 阅读 `task.md`，先画出组件树或数据流。
2. 检查 `params.json`，区分内容、结构、样式与时间。
3. 使用 `prompt.md` 生成第一版。
4. 按 `harness.md` 检查边界。
5. 对照 `solution.tsx` 审计因果结构。
6. 完成 `audit.md` 后才算通过。
